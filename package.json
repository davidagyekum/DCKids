{
  "name": "dckids",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "postinstall": "npm --prefix server ci --omit=dev",
    "start": "npm --prefix server start"
  }
}
