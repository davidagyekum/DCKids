# Secrets — never commit
server/.env
.env
*.env

# Database + WAL sidecars + backups (contain customer/order data)
server/inventory.db
server/inventory.db-wal
server/inventory.db-shm
server/backups/

# Dependencies
node_modules/
server/node_modules/

# Logs / OS / editor cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temp / scratch
tmp/
*.tmp

# Image originals (pre-compression backup — not for production)
_image_originals_backup/

# Local frontend snapshots (taken before design-bundle re-imports) + design bundles
_frontend_backup_*/
_design_tmp*/

# Unused brand/TikTok source content (147MB, not referenced by the app)
dckidsbrand_images/

# Stray databases (agent/runtime, not app data)
*.db
*.db-shm
*.db-wal

# AI agent / tooling runtime (not part of the app)
.claude/
.swarm/
.claude-flow/
.ruflo/
.worktrees/
.superpowers/

# Large unused brand media (TikTok source content)
dckidsbrand_videos/

# Nested duplicate project copy + pre-import backups
dc kids (1)/
_backup_pre_import*/

# Scratch, dev scripts & one-off media (not part of the app)
scratch/
scratch_*.py
scratch_*.png
build_proposal.py
*.pdf
*.MOV
*.mov
IMG_7941.MP4
IMG_8427.PNG
icon.jpg
photo_2026-*.jpg
